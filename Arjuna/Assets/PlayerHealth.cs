using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerHealth : MonoBehaviour
{
    /*
   public static UIManager uiManager;
   private Health health; // Asumsi terdapat kelas Health untuk pemain

    private void Awake()
    {
        health = GetComponent<Health>();
        if (uiManager == null)
        {
            uiManager = this;
        }
        else
        {
            Destroy(gameObject);
        }
    }

    // Method yang memanggil PlayerDied dari UIManager saat pemain mati
    private void PlayerDied()
    {
        health.TakeDamage(health.currentHealth); // Mengurangi health pemain menjadi 0 untuk memastikan mati
        // Kemudian panggil UIManager untuk menangani layar mati atau tindakan lainnya
        UIManager.uiManager.PlayerDied();
    }*/
}
